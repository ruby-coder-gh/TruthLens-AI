# CODEBASE.md
## Project Structure
- `/` — root, empty codebase
  - `.git/` — git repo
  - `prd (1).html` — Product Requirements Document

## Entry Points
None yet — greenfield project.

## Key Modules
None — all to be built.

## Data Model
None — to be designed.

## API Endpoints
None — to be built.

## Key Dependencies
Planned per PRD:
- **Backend:** FastAPI, Python 3.11, JWT auth
- **Frontend:** React + Vite (preferred over Streamlit)
- **AI/LLM:** Ollama (Llama 3.1 8B), sentence-transformers, bge-reranker, LLaVA
- **Retrieval:** ChromaDB, rank_bm25, SQLite
- **Orchestration:** LangChain/LlamaIndex, LangGraph
- **Quality:** RAGAS, Presidio (PII)
- **DevOps:** Docker, GitHub, HF Spaces

## Config
None yet.

## Deployment
None yet.
